[project]
name = "janneau-backend"
version = "0.1.0"
description = "Calculateur de performances thermiques JANNEAU — API REST"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.10",
]
